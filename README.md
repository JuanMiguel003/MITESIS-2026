#Este es mi repositorio de Proceso de tesis para lograr llegar a tener mi titulo de Ing Electrico de mi UCV
#Bajar(PULL) desde Remoto(Github) a Local(mi PC)
git pull origin <nombre_de_la_rama>

Mirar el estado de nuestros archivos
git status 

Listar los commits
git log

#Subir(PUSH) de mi Local(Git) al remoto(Github)
git add .
git commit -m " "
push origin main
